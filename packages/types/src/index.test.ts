import { describe, it, expect } from 'vitest'

describe('Types Package', () => {
  it('should export types correctly', () => {
    // Simple test to ensure the package loads without errors
    expect(true).toBe(true)
  })
}) 