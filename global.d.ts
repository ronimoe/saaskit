import Stripe from 'stripe';
import { jest } from '@jest/globals';

// Note: stripe-server module declarations removed to avoid conflicts with actual implementation

// Note: stripe-sync module declarations removed to avoid conflicts with actual implementation

// Note: database module declarations removed to avoid conflicts with actual implementation 